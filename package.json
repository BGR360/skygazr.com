{
  "name": "skygazr.com",
  "description": "Find the best places to watch the stars",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.6.1",
    "bower": "^1.3.1"
  },
  "scripts": {
    "postinstall": "bower install ; cd app/bower_components/bootstrap ; npm install ; cd ../../..",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000"
  }
}
